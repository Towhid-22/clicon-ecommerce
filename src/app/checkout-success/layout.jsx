import React from 'react'

export const metadata = {
  title: "Checkout Success",
  description: "Clicon ecommerce checkout success page",
};


const layout = ({children}) => {
  return (
    <div>{children}</div>
  )
}

export default layout