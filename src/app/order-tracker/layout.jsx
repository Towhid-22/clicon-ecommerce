import React from 'react'

export const metadata = {
  title: "Order Tracker",
  description: "Clicon ecommerce order tracker page",
};


const layout = ({children}) => {
  return (
    <div>{children}</div>
  )
}

export default layout