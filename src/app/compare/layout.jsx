import React from 'react'

export const metadata = {
  title: "Compare",
  description: "Clicon ecommerce compare page",
};


const layout = ({children}) => {
  return (
    <div>{children}</div>
  )
}

export default layout