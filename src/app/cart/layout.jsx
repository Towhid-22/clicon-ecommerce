import React from 'react'

export const metadata = {
  title: "Cart",
  description: "Clicon ecommerce cart page",
};


const layout = ({children}) => {
  return (
    <div>{children}</div>
  )
}

export default layout